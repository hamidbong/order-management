pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        echo "🔨 Build de ServiceA"
        // Ton build spécifique ici
      }
    }
    stage('Tests') {
      steps {
        echo "🧪 Tests de ServiceA"
        // Tes tests ici
      }
    }
    stage('Déploiement') {
      steps {
        echo "🚀 Déploiement de ServiceA"
        // Ton déploiement ici
      }
    }
  }
}
