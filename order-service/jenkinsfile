pipeline {
  agent any
  tools {
        nodejs 'NodeJS' // Match the name you configured
    }

  stages {
    stage('Build') {
        steps {
            echo "🔨 Build de ServiceA"
            sh 'npm install'
        }
    }
    stage('Tests') {
      steps {
        echo "🧪 Tests de ServiceA"
        // Tes tests ici
        
      }
    }
    stage('Déploiement') {
      steps {
        echo "🚀 Déploiement de ServiceA"
        // Ton déploiement ici
      }
    }
  }
}
