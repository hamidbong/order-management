pipeline {
  agent any

  stages {
    stage('Checkout') {
            steps {
                checkout scm  // This pulls your Git repository
            }

    stage('Build') {
        steps {
            echo "🔨 Build de ServiceA"
            dir('order-service') {  // Change to the subdirectory
                sh 'npm install'
            }
        }
    }

    stage('Tests') {
      steps {
        echo "🧪 Tests de ServiceA"
        // Tes tests ici
        
      }
    }
    stage('Déploiement') {
      steps {
        echo "🚀 Déploiement de ServiceA"
        // Ton déploiement ici
      }
    }
  }
}
